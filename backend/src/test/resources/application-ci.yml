# credentials for test database
spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: "jdbc:postgresql://db:5432/blokatugent_test"
    username: postgres
    password: postgres
  flyway: # For automatic flyway migration integration with spring boot
    url: "jdbc:postgresql://db:5432/blokatugent_test"
    user: postgres
    password: postgres
