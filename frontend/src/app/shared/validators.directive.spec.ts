import { ValidatorsDirective } from './validators.directive';

describe('ValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
